package eu.europeana.api.dataset.generation.model;

/**
 * Enum representing various types of storage mechanisms available for datasets.
 * Each storage type corresponds to a specific method or platform used for
 * persisting dataset information.
 */
public enum StorageType {
        FILE,
        IBM,
        DATABASE,
        S3;
}
